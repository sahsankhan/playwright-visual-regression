{
  "name": "playwright-visual-regression",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright built-in visual regression against Toolshop UI.",
  "scripts": {
    "test": "node src/utils/run.js --grep @smoke",
    "test:headed": "node src/utils/run.js --headed --grep @smoke",
    "test:smoke": "node src/utils/run.js --grep @smoke",
    "test:update-snapshots": "playwright test --update-snapshots",
    "report": "playwright show-report",
    "report:summary": "node src/utils/visualReport.js",
    "demo:diff": "node scripts/capture-readme-diff.js",
    "test:update-snapshots:docker": "docker run --rm -v \"${PWD}:/work\" -w /work mcr.microsoft.com/playwright:v1.55.0-noble bash -lc \"npm ci && npx playwright test --grep @smoke --update-snapshots\""
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.7"
  }
}
